# No lockfile at the workspace root. This is not a monorepo - each app owns its own
# package-lock.json, and a root lockfile makes Next.js pick "E:\sadamata new" as the
# inferred workspace root, which breaks outputFileTracing for every app.
package-lock=false
